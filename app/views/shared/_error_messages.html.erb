<% if object.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-error">
      Kayıt formunda <%= pluralize(object.errors.count, "hata")%> bulunmaktadır.
    </div>
    <ul>
    <% object.errors.full_messages.each do |msg| %>
      <li>* <%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>