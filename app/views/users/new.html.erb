<% provide(:title, 'Kayıt') %>
<h1>Kayıt ol</h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for(@user) do |f| %>
      <%= render 'shared/error_messages', object: f.object %> <!-- Display error messages for invalid signup form-->

      <%= f.label :name %>
      <%= f.text_field :name %>

      <%= f.label :email %>
      <%= f.text_field :email %>

      <%= f.label :password %>
      <%= f.password_field :password %>

      <%= f.label :password_confirmation%>
      <%= f.password_field :password_confirmation %>

      <%= f.submit "Hesabımı Yarat", class: "btn btn-large btn-primary submit_btn_loc" %>
    <% end %>
  </div>
</div>